{
  "name": "rodeo-os",
  "private": true,
  "version": "0.1.0",
  "description": "RodeoApps.pro OS — the operating system for producing a rodeo.",
  "workspaces": ["packages/*", "apps/*"],
  "engines": { "node": ">=22.18" },
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
