{
  "name": "@rodeo-os/web",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Secretary interface. No bundler, no dependencies, no build step.",
  "scripts": {
    "dev": "node --watch server.ts",
    "start": "node server.ts",
    "typecheck": "tsc --noEmit",
    "test": "node test/views.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
